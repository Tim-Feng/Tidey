| type | test | seam | minimal change | commit kind | status |
| --- | --- | --- | --- | --- | --- |
| structural | `TideyLoginShellCommandTests/*` | login-shell env preservation depends on command selection, not shell integration; default profiles need a direct test on `ITAddressBookMgr` command generation | add tests that assert default login-shell profiles use `ShellLauncher` instead of `login -fp` | STRUCTURAL | pass |
| behavior | `TideyLoginShellCommandTests/testDefaultLoginShellUsesShellLauncherCommand` | `loginShellCommandForBookmark:` should preserve Tidey env in the default home-directory/default-shell case | route default login-shell profiles through `shellLauncherCommandWithCustomShell:nil` | BEHAVIORAL | pass |
| behavior | `TideyLoginShellCommandTests/testCustomShellLoginUsesShellLauncherCommandWithCustomShell` | custom shell login must keep existing shell selection while preserving Tidey env | keep `SHELL=/path` behavior when generating the `ShellLauncher` command | BEHAVIORAL | pass |
