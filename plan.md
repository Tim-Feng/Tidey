| type | test | seam | minimal change | commit kind | status |
| --- | --- | --- | --- | --- | --- |
| structural | `TideyEditorExternalChangeTests/*` | the current tests depend on the full `tideyOpenEditorFileAtPath:` flow, but external-change behavior only needs a current-tab seam | add a direct test seam for setting current editor tabs/selection and syncing the watcher without going through panel/file-tree open flow | STRUCTURAL | pass |
| behavior | `TideyEditorExternalChangeTests/testExternalChangeReloadsCurrentCleanTabFromDisk` | current file loading already reads disk content into `tab.content` and pushes it into Monaco when a tab is selected | when the active clean tab's file changes externally, reload its on-disk contents into the tab and push the new value into Monaco | BEHAVIORAL | pass |
| behavior | `TideyEditorExternalChangeTests/testExternalChangeDoesNotClobberDirtyCurrentTab` | current editor tabs can be dirty via `tideyEditorDidChangeValue:` | when the active tab has unsaved edits, ignore the external file change so local edits are not overwritten | BEHAVIORAL | pass |
| behavior | `TideyEditorExternalChangeTests/testEditorFileWatcherRetargetsWhenSelectedTabPathChanges` | selecting or closing an editor tab already updates `_tideyEditorLoadedPath` and current tab state | when the selected saveable tab changes, stop watching the old file and start watching the new one so external reloads follow the active editor file | BEHAVIORAL | pass |
