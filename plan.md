| type | test | seam | minimal change | commit kind | status |
| --- | --- | --- | --- | --- | --- |
| structural | `TideyEditorFileTreeWatcherTests/*` | `iTermRootTerminalView` rebuilds the file tree on demand, but there is no testable watcher seam yet for root-path file system changes | introduce a start/stop/handle-change seam for the watched file-tree root so tests can drive refresh logic without a live `FSEventStream` | STRUCTURAL | pass |
| behavior | `TideyEditorFileTreeWatcherTests/testFileTreeReloadsWhenWatchedRootAddsFile` | existing `reloadTideyEditorFileTree` rebuilds `_tideyEditorFileTreeRootNode`, but nothing calls it on file-system changes | when the watched root reports a change and a new file now exists, rebuild the file tree so the new child appears without manual collapse/re-expand | BEHAVIORAL | pass |
| behavior | `TideyEditorFileTreeWatcherTests/testFileTreeReloadsWhenWatchedRootDeletesFile` | same watcher callback seam as the add-file case | when the watched root reports a change and a file disappeared, rebuild the file tree so the removed child disappears without manual collapse/re-expand | BEHAVIORAL | pass |
| behavior | `TideyEditorFileTreeWatcherTests/testFileTreeWatcherRetargetsWhenRootPathChanges` | file-tree root path already changes through `tideyEditorFileTreeRootPath` and `reloadTideyEditorFileTree` | when the visible file-tree root changes, stop watching the old root and start watching the new root so updates follow the current tree | BEHAVIORAL | pass |
